<% if (models.length > 0){%>
    {{{ if(auth){ }}}
        {{{- menu.vertical([
            <% for (var m in models){ %>
            {
                label: '<%= models[m].label %>',
                childs: [
                    {
                        label: 'List',
                        target: '/<%= models[m].camelName %>'
                    }
                ]
            },
            <% } %>
        ], {rootCssClass:'nav nav-tabs nav-stacked',subCssClass:'dropdown-menu', id:'tabSettings'}) }}}
    {{{ } }}}
<% } %>