<div class="dataList" ng-controller="userController" ng-init="clientId = <%= JSON.stringify(clientId) %>; 
     userColumns = <%= JSON.stringify(userColumns) %>; init();" >
    <div class="row-fluid span12 dataCommands">
        <%- html.tag('a','Add User',{'class':'btn btn-primary', role:'button', 'data-toggle':'modal',
        href:'#userForm', 'ng-click':'user = null'})%>
    </div>
    <%- include form%>
    <div class="row-fluid ">
        <div grid></div>
    </div>
</div>
