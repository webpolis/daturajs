<div class="dataList" ng-controller="vendorController" ng-init="clientId = <%= JSON.stringify(clientId) %>; 
     vendorColumns = <%= JSON.stringify(vendorColumns) %>; init();" >
    <div class="row-fluid span12 dataCommands">
        <%- html.tag('a','Add Vendor',{'class':'btn btn-primary', role:'button', 'data-toggle':'modal',
        href:'#vendorForm', 'ng-click':'vendor = null'})%>
    </div>
    <%- include form%>
    <div class="row-fluid ">
        <div grid></div>
    </div>
</div>
